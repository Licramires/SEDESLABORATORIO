@{
    ViewData["Title"] = "Acceso denegado";
}

<div class="container py-5 text-center">
    <h1>Acceso denegado</h1>
    <p class="text-muted">Tu rol no tiene permisos para acceder a este recurso.</p>
    <a asp-area="auth" asp-controller="Auth" asp-action="Dashboard" class="btn btn-primary">Volver al panel</a>
</div>
