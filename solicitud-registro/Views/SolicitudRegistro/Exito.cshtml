@{
    ViewData["Title"] = "Solicitud Enviada";
}

<div class="container mt-5 text-center">
    <h2 class="text-success">¡Solicitud enviada con éxito!</h2>
    <p>Tus datos y documentos han sido registrados correctamente para su revisión.</p>
    @if (TempData["SolicitudCreada"] is not null)
    {
        <p class="text-muted">Número de solicitud: <strong>#@TempData["SolicitudCreada"]</strong></p>
    }
    
    <a asp-area="solicitud-registro" asp-controller="SolicitudRegistro" asp-action="Nueva" class="btn btn-secondary mt-3">
        Volver a enviar otra solicitud
    </a>
</div>